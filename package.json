{
  "name": "ss220-webmap",
  "version": "1.0.0",
  "scripts": {
    "start": "vite",
    "build": "vite build --base=./",
    "serve": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "ol": "latest"
  }
}
